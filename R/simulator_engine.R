# Calculate recovery paths and summary indicators.
simulate_recovery_path <- function(series_data, benchmark_year, growth_rate, anchor_year = NULL) {
  stop("Not implemented: compute normalized recovery paths and recovery timing.")
}

# Return status flags for edge cases in the simulator.
classify_recovery_status <- function(simulation_output) {
  stop("Not implemented: classify already recovered and other status flags.")
}

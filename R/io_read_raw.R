# Read raw project inputs.
read_raw_historical_data <- function(path = "data/raw/milagros_economicos.xlsx") {
  stop("Not implemented: read the raw Excel workbook and return the historical series.")
}

# Placeholder for the main simulator module.
simulator_module_ui <- function(id) {
  stop("Not implemented: build the simulator UI module.")
}

simulator_module_server <- function(id) {
  stop("Not implemented: build the simulator server module.")
}
